services:
  object-viewer-client:
    build:
      context: ./
      dockerfile: Dockerfile.production
    restart: always
    ports:
      - 3000:80
    networks:
      - object-viewer-net

networks:
  object-viewer-net:
