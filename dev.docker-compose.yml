services:
  object-viewer-client:
    build:
      context: ./
      dockerfile: Dockerfile.development
    restart: always
    volumes:
      - .:/home/app
      - /home/app/node_modules
    ports:
      - 3000:3000
    networks:
      - object-viewer-net
    tty: true

networks:
  object-viewer-net:
