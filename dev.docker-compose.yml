services:
  orbita-client:
    build:
      context: ./
      dockerfile: Dockerfile.development
    restart: always
    volumes:
      - .:/home/app
      - /home/app/node_modules
    ports:
      - 3000:3000
    networks:
      - orbita-net
    tty: true

networks:
  orbita-net:
